{
  "manifest_version": 2,

  "name": "Show Grid Lines",
  "description": "This extension allows the user view one grid line in current page.",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab"
  ]
}
